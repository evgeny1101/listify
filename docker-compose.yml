services:
  bot:
    container_name: listify
    build: .
    restart: unless-stopped
    volumes:
      - ./listify.db:/app/listify.db
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ALLOWED_USERS=${ALLOWED_USERS}